#include <stdio.h>
/**
* main - show two dimensional array
*
* return: always 0
*/

int main(void)
{
	int a[2][3] = {2,3,1,4,5,6};

	int i,j;

	for (i - 0; i < 2; i++)
	{
		for (j = 0; j< 3; j++)
		{
			printf("a[%d][%d] = %d\n", i,j,a[i][j]);
		}
	}
	return (0);
}

